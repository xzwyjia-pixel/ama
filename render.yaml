services:
  - type: web
    name: zhenaudit-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn ruleguard_server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: DEEPSEEK_API_KEY
        sync: false
    healthCheckPath: /health
